name: SP-ProxyData
version: '${version}'
main: net.teujaem.spProxyData.SPProxyData
api-version: '${spigot_api_version}'
depend:
  - SP-Framework
commands:
  broadcast:
    permission: spporxydata.broadcast